# TODO - Finish Gov (Officer) Dashboard

## Step 1
Inspect existing dashboard + related services/auth to understand what’s missing.

## Step 2
Add real officer dashboard data wiring using existing `fetchReports()` + `voteReport()`/`addComment()`/new officer actions.

## Step 3
Implement officer actions: verify/resolve/assign reports and update localStorage via new API functions.

## Step 4
Add officer UI: tabs (Queue, Assigned, Resolved), filters by department, search/sort by priorityScore.

## Step 5
Ensure role gating + loading states are correct.

## Step 6
Run dev/build to confirm no runtime errors.

